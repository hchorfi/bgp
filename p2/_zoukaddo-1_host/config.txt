# Host 1 (zoukaddo-1) VXLAN configuration
# This host is part of the VXLAN overlay network with ID 10
# It communicates through the bridge br0 which connects to the VXLAN tunnel

# Configure the local interface with IP in the overlay network
ip addr add 192.168.10.1/24 dev eth0
ip link set eth0 up

# Test connectivity within the VXLAN
# ping 192.168.10.2
